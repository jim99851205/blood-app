## 計劃：血壓管理 App 佈景主題切換與持久化

TL;DR：在現有血壓管理 App 的基礎上，新增首頁右上角的 dark/light 主題切換開關，採用 CSS 變數統一管理兩種主題顏色，並將使用者選擇儲存在 localStorage，讓重新載入頁面後仍維持選擇的主題，同時保留原本血壓紀錄功能不受影響。

### 步驟
1. 確認現有頁面結構與資料流，確認血壓表單、紀錄列表與 localStorage 保存邏輯仍然正常，並將這些功能視為主題切換的基礎。
2. 在首頁右上角新增佈景主題切換開關，採用圖示與文字一起顯示，方便使用者一眼辨識目前狀態。
3. 以 CSS 變數定義兩套主題色票：Light 與 Dark，集中控制背景、文字、卡片、按鈕、邊框、標籤與表單的顏色。
4. 在 JavaScript 中加入主題初始化邏輯，優先讀取 localStorage 中保存的選擇；若沒有資料，則使用預設深色主題。
5. 實作切換事件，當使用者點選主題開關時，更新頁面主題並立即寫回 localStorage，讓設定持久化。
6. 驗證不同主題下的頁面一致性，尤其是血壓紀錄列表、表單輸入框、主按鈕與文字對比，避免在深色／淺色模式下造成閱讀困難。
7. 進行本機瀏覽器驗證，確認：預設深色主題、切換亮暗模式、刷新頁面後仍保留選擇、原本新增紀錄功能不受影響。

### 相關檔案
- [index.html](index.html) — 新增首頁右上角主題切換開關與頁首容器。
- [style.css](style.css) — 定義 Light/Dark 色票、切換樣式與整體版面。
- [app.js](app.js) — 管理 localStorage、初始化主題與切換事件，以及保留血壓紀錄邏輯。

### 驗證
1. 打開頁面確認右上角主題開關正常顯示。
2. 點擊主題開關切換成淺色與深色，確認整體介面與文字可讀性正常。
3. 刷新頁面，確認主題仍維持上次選擇。
4. 新增一筆血壓紀錄，確認紀錄列表仍然正常顯示。
5. 確認所有原本功能未被破壞，頁面在兩種主題下均可正常使用。

### 決策
- 預設主題：深色（Dark）
- 儲存方式：localStorage
- 主題開關位置：首頁右上角
- 設計風格：簡約現代風
- 切換介面：圖示 + 文字
- 範圍：僅新增佈景主題切換與持久化，不擴大到其他功能

### 進一步考量
1. 若未來要擴充更多主題，例如「高對比模式」或「護眼模式」，目前的 CSS 變數設計能相容擴充。
2. 若需要更完整的手機版體驗，下一步可再微調按鈕尺寸與導航排版。
